const { expect } = require('chai');
const lib = require('../src/runner');

// Runner
describe('Runner', () => {
  describe('createDesign()', () => {
    it('expect "createDesign()" to not throw with arguments', (done) => {
      // expect(lib({ platform: 'android', app: 'com.timoa.ciremote',})).to.not.throw();
      done();
    });
  });
});
